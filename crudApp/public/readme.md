//my personal readme
npm run buid para gerar denovo o css, js publicos apos mudanca isso com blade

Um ataque CSRF acontece quando um site malicioso tenta fazer o navegador do usuário enviar uma requisição para o seu sistema sem que ele perceba, usando a sessão já autenticada.

php artisan storage:link para gerar link das imagens no storage que estao no public

php artisan vendor:publish --tag=laravel-mail -> para personalizar o nosso email

//to install
composer install
cp .env.example .env
php artisan key:generate
touch database/database.sqlite
php artisan migrate
php artisan storage:link